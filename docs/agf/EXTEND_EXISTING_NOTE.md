# AF-22, SYS-01→10 — Extend Existing (no separate formation)

```
11 rows, per AGENT_FACTORY_IOS_BRAIN_CMD_LAURENTIA_RECONCILIATION.md's
own EXTEND_EXISTING/MERGE verdicts — reused as-is, never rebuilt.
```

## AF-22 — Agent Authority, Delegation & Escalation

Per the reconciliation: coverage `NONE`, distinctness
`DISTINCT_OPERATOR_ROLE`, action `EXTEND_EXISTING` — "reuse
`00_GOVERNANCE/AUTHORIZATION_MODEL.md` (already built) rather than
reinventing agent authority/delegation/escalation doctrine — same
reuse pattern as FRK-45/46."

**No separate formation is built.** A future candidate needing AF-22
competency is pointed at `00_GOVERNANCE/AUTHORIZATION_MODEL.md`
directly.

## SYS-01→10 — Intelligent Operations Pipeline

Per the reconciliation: coverage `SUBSTANTIAL`, distinctness
`NOT_DISTINCT` from existing doctrine, action `EXTEND_EXISTING`/
`MERGE` — "this is the same pipeline, stage by stage, as
`80_MISSIONS/MISSIONS_PIPELINES.md`'s already-documented 'Intelligent
Operations' pipeline (humain→Laurentia→IOS→Brain→Agent Factory→
application→Proof→Command Center→humain). Reuse that doctrine; do not
rebuild 10 formations restating its 10 stages."

**No separate formations are built for SYS-01→10.** A future candidate
needing this competency is pointed at `80_MISSIONS/
MISSIONS_PIPELINES.md` directly — the same document `XCV-57→66`
already converges on (`XCV_TRANSVERSAL_RECONCILIATION.md`, `G10`).

## Status

Both entries: `EXTEND_EXISTING`, no `docs/agf/` content authored, per
the reconciliation's own verdict — never reclassified here.
