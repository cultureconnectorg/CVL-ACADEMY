# BRN-01→14 — AI/ML Reasoning Disciplines (external/market)

## Grounding

Per `AGENT_FACTORY_IOS_BRAIN_CMD_LAURENTIA_RECONCILIATION.md`:
coverage `NONE`, distinctness `DISTINCT_PROFESSION` ("real AI/ML
disciplines: context interpretation, retrieval, reasoning,
recommendation, planning are genuine market-general knowledge"),
action `NEW_EXTERNAL`. "`CAPABILITY_NOT_IMPLEMENTED` for every
CVLN-Brain-specific claim beyond the one real touchpoint" (that one
touchpoint is BRN-15, in `internal/`, not here).

## Objectives

- Teach the real, current AI/ML reasoning disciplines this cluster
  covers: context interpretation, retrieval-augmented reasoning,
  recommendation systems, planning/reasoning engines — as
  industry-general knowledge.
- Never claim any CVLN "Brain" system implements these — the only
  real CVLN-Brain touchpoint this Academy has is `academy.
  certification.passed` (BRN-15, `internal/`), a single event
  subscription, nothing resembling a reasoning engine.

## Modules

1. Context-interpretation fundamentals.
2. Retrieval-augmented reasoning patterns.
3. Recommendation-system design.
4. Planning/reasoning-engine architecture.
5. CVLN-gap discipline — the single real touchpoint (BRN-15) is
   explicitly not a reasoning engine.

## Assessment

A discipline-literacy exam graded against real AI/ML practice, with an
eliminatory check on inflating BRN-15's single event subscription into
a reasoning engine.

## Status

`STATUS = PACKAGE_COMPLETE` — full canonical package, deepened this
pass. Never implies `FULLY_COMPLETE`.
