# AF-X-03 — Academy Skill/Certification × Agent Qualification Bridge

## Repo truth this formation is built on

Grounded in this Academy's own real, already-built skill/certification
engine (`docs/kor/`'s Skill ID architecture, `70_EVIDENCE/
EVIDENCE_ARCHITECTURE.md`) and real persona system (`ai_assistant.py`).
Per `AGENT_FACTORY_IOS_BRAIN_CMD_LAURENTIA_RECONCILIATION.md`:
coverage `PARTIAL (Academy's real skill/certification engine + real
persona system both exist, just not wired as 'agent qualification')`,
action `NEW_CROSS_ECOSYSTEM`, **"best-grounded of the 9 AF-X
bridges."**

**Market-context, never the operating substrate**: the real external
`frekcore/CVLNAgentfactory`'s ADL (`AGT-\d{3}` ids, semver, 7-stage
lifecycle, `allowed_transitions()`) is the real analog of an "agent
qualification" system elsewhere in the CVLN ecosystem — cited here
only as evidence the concept is real and substantial, never as
something this Academy can wire into today.

## Prerequisites

AF-16, AF-17.

## Objectives

- Teach the conceptual bridge between this Academy's real skill/
  certification machinery and the idea of "agent qualification" —
  both real building blocks exist (Skill IDs, evidence models,
  personas), but **no wiring connects them to any agent-qualification
  concept** today.
- Cite `CVLNAgentfactory`'s real ADL lifecycle as evidence this bridge
  is a real, substantial future direction in the ecosystem — never as
  a claim that this Academy already qualifies agents.

## Modules

1. Academy skill/certification engine literacy (by reference to
   `docs/kor/`'s established architecture).
2. Agent-qualification concept literacy, grounded on `CVLNAgentfactory`'s
   real ADL as market-context.
3. "Not yet wired" discipline — explicit statement that no bridge
   exists today between the two real building blocks.

## Assessment

A bridge-conceptualization exercise: candidate documents what a real
agent-qualification bridge would require, citing only real existing
pieces on both sides — eliminatory failure for claiming the bridge
already operates.

## Evidence / mission eligibility

`AFX03.SKILL.*` reserved once deepened. No mission eligibility path
exists.

## Status

`STATUS = PACKAGE_COMPLETE` — full canonical package, deepened this
pass. Never implies `FULLY_COMPLETE`.
