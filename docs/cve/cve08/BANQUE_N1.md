# CVE-08 — Banque N1 (formative, M1→M3)

```
Sourced directly against KORA_CVE_Specification_Mathematique_v1.0.md
§3.3 and §6, re-read in full this session.
```

## M1 — VCF's two real appearances

1. Quote VCF's first real appearance in the document, exactly.
   (§3.3: `v_i(t) = dVCF_i/dt`, "instantaneous rate of incremental
   value creation" for work `i`)
2. Quote VCF's second real appearance, exactly. (§6, the Fundamental
   Law's objective: `Maximize: Σ_i VCF_i^{corrected}(t)`)
3. What does §3.3's usage assume about VCF? (That it is a
   differentiable function of time, whose rate `v_i(t)` feeds CHL peak
   detection)

## M2 — formalization-gap verification

4. Does any equation defining VCF itself (in terms of `S,E,F,C,N` or
   any other primitive) appear anywhere in the document? (No — direct
   verification this session confirms it does not; VCF is assumed as a
   given input in both appearances)
5. Is this a genuine gap, or an oversight to paper over? (A genuine,
   named formalization gap — never invented or silently filled)

## M3 — working-hypothesis discipline

6. What is the closest fully-defined "value" quantity in the document
   that could serve as a working-hypothesis proxy for VCF at the cycle
   level? (`CVI_i,c`, §3.1)
7. If a candidate proposes `VCF_i(t) ≈ CVI_i,c` as a working
   hypothesis, how must it be labeled? (Explicitly as `HYPOTHESIS, NOT
   SPEC` — never presented as something the document itself states)
