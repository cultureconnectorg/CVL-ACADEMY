# CVE-10 — Banque N1 (formative, M1→M3)

```
Sourced directly against KORA_CVE_Specification_Mathematique_v1.0.md
§5 and §6 C1, re-read in full this session. Same formula as CVE-09,
revenue-policy framing.
```

## M1 — `MD_c` as a real revenue pool

1. Does the document define how `MD_c`'s real-world € value is set for
   a given cycle? (No — it defines only how a fixed `MD_c` is
   *distributed* once set, never how its monetary value is
   determined)
2. What does the document define instead? (The distribution mechanism
   — `UVC_i,c = (CVI_i,c/Σ_j CVI_j,c)·MD_c` — given `MD_c` as an
   input)

## M2 — zero-sum-within-cycle discipline

3. What does constraint C1 guarantee? (`Σ_i UVC_i,c = MD_c` — the
   total allocated always exactly equals the fixed pool, no more, no
   less)
4. If one work's `CVI_i,c` share of the cycle total increases (all
   else equal), what happens to every other work's `UVC_j,c`? (Their
   relative shares of the fixed `MD_c` necessarily decrease — the
   pool is zero-sum within a cycle)

## M3 — cross-reference to CVE-09

5. Is the `UVC_i,c` formula re-derived in CVE-10, or cited by
   reference? (Cited by reference to CVE-09 — never re-derived here,
   per the corpus's anti-duplication discipline, §26)
