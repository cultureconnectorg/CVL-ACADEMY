# CVE-11 — Banque N1 (formative, M1→M3)

```
Sourced directly against KORA_CVE_Specification_Mathematique_v1.0.md
§5 and §6 C5, re-read in full this session. Same formula as CVE-09,
creator-facing framing.
```

## M1 — creator-facing UVC explanation

1. In creator-facing terms, what determines a creator's `UVC_i,c`?
   (Their work's `CVI_i,c` — itself built from the components taught
   in CVE-02/04/05/07 — as a share of the cycle's total CVI, scaled
   by `MD_c`)
2. Is the CES/UVC formula re-derived in CVE-11, or cited by
   reference? (Cited by reference to CVE-09 — never re-derived, per
   the corpus's anti-duplication discipline, §26)

## M2 — cultural-neutrality guarantee, precisely scoped

3. State constraint C5 exactly. (`E[UVC_i | CVI_i = v, culture_i = A]
   = E[UVC_i | CVI_i = v, culture_i = B] ∀A,B` — cultural neutrality)
4. What does C5 guarantee? (Equal expected UVC for equal CVI,
   regardless of culture)
5. What does C5 NOT guarantee? (Equal CVI itself — CVI still depends
   on real engagement/attribution/circulation signals, which can
   differ across works for legitimate reasons unrelated to culture)

## M3 — scope discipline

6. Does the document define a real mechanism for setting `MD_c`'s
   monetary value? (No — see CVE-10; never promise a creator a
   specific payout figure on that basis)
