# CVE-12 — Cultural Network Economics

```
Prerequisite: CVE-07 (specializes the Nebula Score's network axes).
```

## Repo truth

§3.2's Nebula Score, specifically the network-relevant axes among the
6 named (`territory`, `diaspora`, `collaboration`) and the velocity
factor `φ(i,c)` as a measure of network-coverage expansion speed.

## Prerequisites

CVE-07 — this formation specializes on the network-economics reading
of the same Nebula Score formula, it does not re-derive the entropy
or novelty-coefficient mechanics (covered in CVE-07).

## Objectives

1. Explain why `territory`, `diaspora`, and `collaboration` are
   specifically network-economics axes (they measure cultural spread
   across social/geographic/collaborative structures, not individual
   listening intensity).
2. Explain the velocity factor `φ(i,c)` as a network-growth-rate
   signal specifically.
3. Never invent a network-economics concept (e.g. a formal graph
   centrality measure, a viral-coefficient formula) beyond what §3.2
   actually defines.

## Modules

| # | Module | Real grounding | Deliverable |
|---|---|---|---|
| M1 | Network-axis specialization | §3.2, 3 of the 6 axes | Written note: why these 3 axes specifically read as "network economics" |
| M2 | Velocity-as-network-growth literacy | `φ(i,c)` | Applied interpretation note |
| M3 | Scope discipline | No graph-theoretic formula in the spec | Honest note: this is entropy + novelty + velocity, never a formal network-science model beyond that |

## Assessment

Per `../CERTIFICATION_MODEL.md`.

## Evidence / certification / mission eligibility

Same general pattern as CVE-01.

## Status

`STATUS = MODULE_CONTENT_DRAFTED`.
