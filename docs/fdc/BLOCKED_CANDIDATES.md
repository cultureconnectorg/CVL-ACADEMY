# Fondation Cœurvolan — Blocked Candidates (`BLOCKED_PRODUCT_DEPENDENCY`, FDC-36→48, 13 rows)

Per the reconciliation: `PARTIAL` if the CIP/Fondation identity
question resolves positive (`CIP-01` anchors it — it does not, per
`FD-CIP-001`: the two stay distinct), else `NONE`. `DISTINCT_
OPERATOR_ROLE` each, `NEW_INTERNAL`, mostly `BLOCKED_PRODUCT_
DEPENDENCY` — **no archive/grant/governance system exists in any repo
audited this session.**

**Mandatory caution, restated per-row where institutional authority is
implied** (real institutional authority never simulated):

| Row | Title | Note |
|---|---|---|
| FDC-47 | Foundation Governance Operator | Same caution as `GRP-62`/`GRP-69`/`GRP-70` (`docs/grp/BLOCKED_CANDIDATES.md`). |
| FDC-48 | Foundation Audit & Accountability | Same caution. |
| (11 other FDC-36→48 rows) | Internal-restricted foundation operations | `BLOCKED_PRODUCT_DEPENDENCY`, no capability simulated. |

**No capability simulated for any of these 13 rows.**
