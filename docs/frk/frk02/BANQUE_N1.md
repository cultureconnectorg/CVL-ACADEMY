# FRK-02 — Banque N1

```
Sourced against frek_core.py's own docstring and the explicit scope
discipline of FRK-02's own REFERENTIAL.md.
```

1. Cite la phrase du docstring de `frek_core.py` qui définit son rôle
   architectural. ("the sole boundary through which the app talks to
   FrekCore")
2. FRK-02 peut-il enseigner une architecture DID/VC/provenance-graphe
   comme si elle était construite dans ce repo ? (Non — ces concepts
   n'existent nulle part dans ce repo, seulement dans le candidat map
   Master 2D)
3. Quelle est la différence d'altitude entre FRK-01 et FRK-02 ?
   (FRK-01 = system map opérationnel des 5 méthodes ; FRK-02 =
   positionnement architectural conceptuel de FREKCORE parmi les
   autres systèmes CVLN)
4. `frek_core.py` mentionne-t-il explicitement Wallet, KORA ou Agent
   Factory comme systèmes connectés ? (Non — aucune mention de
   connexion réelle observée)
5. Que doit faire un candidat qui ne trouve aucune preuve d'une
   architecture demandée ? (Déclarer `CAPABILITY_NOT_IMPLEMENTED`,
   jamais inventer)
