# FRK-07 — Banque N1

1. FRK-07 enseigne-t-il une architecture IAM CVLN réelle ? (Non —
   discipline marché-générale uniquement)
2. Quelle est la frontière avec FRK-06 ? (FRK-06 = minting séquentiel
   étroit ; FRK-07 = architecture IAM complète, marché-générale)
3. `frek_core.py` implémente-t-il OAuth2/OIDC ? (Non — aucune trace)
4. Un candidat peut-il affirmer que `mint_frek_id()` est un IdP
   complet ? (Non — élimination si affirmé)
