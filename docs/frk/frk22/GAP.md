# FRK-22 — .fk Packaging & Validation — GAP

```
STATUS = BLOCKED_PRODUCT_DEPENDENCY. No content built.
```

## Why nothing is built here

Same blocker as FRK-21 — the `.fk` format itself is not defined
anywhere in this repo, so its packaging/validation layer cannot be
taught against real specifics either.

## What would need to exist first

Same as FRK-21: a real `.fk` format specification.

## Status

`STATUS = BLOCKED_PRODUCT_DEPENDENCY`, unchanged from the
reconciliation layer.
