# FRK-26 — Evidence Model

`READY_FOR_FREK_PROOF = FALSE`.

## Chaîne de preuve

Graphe de provenance annoté (acyclique, horodaté) → note de gap CVLN →
correcteur → (jury si 2.0–2.5) → `FRK26.SKILL.PROVENANCE_GRAPH.L1`
(réservé).

## Ce qui compte comme preuve

Un graphe acyclique et horodaté correctement conçu pour une chaîne de
dérivation ; la distinction correcte entre arêtes de dérivation
(acycliques) et autres relations (potentiellement cycliques).

## Ce qui NE compte PAS comme preuve

Un graphe cyclique pour une chaîne de dérivation ; toute affirmation
qu'un système CVLN maintient un graphe de provenance réel.

- Réservation d'ID : `FRK26.SKILL.PROVENANCE_GRAPH.L1` — réservé, non
  émis.
- Base pour FRK-27 (spécialisation knowledge-graph) — réutilisé par
  référence.
- Mapping `VALID_SIGNALS` : aucun aujourd'hui.
