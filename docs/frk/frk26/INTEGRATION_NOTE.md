# FRK-26 — Integration Academy Package Note

## Réel vs. supposé

**Réel :** la discipline de modélisation de graphes de relations et de
provenance réelle (acyclicité des arêtes de dérivation, horodatage,
distinction avec les relations non-dérivatives) comme pratique
marché-générale. Aucun graphe de provenance CVLN observé.

**Supposé :** `FRK26.SKILL.*` réel dans le runtime de cette Academy —
inexistant (`NO_RUNTIME_BINDING`). Tout graphe de provenance CVLN
réel — jamais accordé (`CAPABILITY_NOT_IMPLEMENTED`).

## Dépendances

- `FREK_01_75_RECONCILIATION.md`, `docs/frk/frk27/REFERENTIAL.md`
  (spécialisation avale, réutilisée par référence), `70_EVIDENCE/
  EVIDENCE_ARCHITECTURE.md`.

## Ce qu'une future intégration exigerait

1. Une entrée `FRK26` dans le registre de certification de cette
   Academy, namespace distinct de `FRK27.SKILL.*`.
2. Une surface candidat réelle (ce corpus est markdown seul).
3. Un vrai graphe de provenance à évaluer par un correcteur humain —
   inexistant aujourd'hui.

## Status

`STATUS = PACKAGE_COMPLETE` — référentiel, banques N1/N2, assessment
+ rubric, evidence model, 3 guides, cette note d'intégration existent
tous, deepened this pass. `FULLY_COMPLETE` non déclaré — requiert un
passage réel vérifié par un humain.
