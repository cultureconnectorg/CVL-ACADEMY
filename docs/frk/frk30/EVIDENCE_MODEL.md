# FRK-30 — Evidence Model

`READY_FOR_FREK_PROOF = FALSE`.

## Chaîne de preuve

Pipeline d'empreinte annoté (extraction, vecteur, stockage/indexation)
→ note de référence FRK-29 → justification des choix d'ingénierie →
correcteur → (jury si 2.0–2.5) →
`FRK30.SKILL.FINGERPRINT_ENGINEERING.L1` (réservé).

## Ce qui compte comme preuve

Un pipeline complet et cohérent avec des choix d'ingénierie justifiés
(dimensionnalité, normalisation, stockage/indexation) ; la référence
correcte et non-dupliquée à FRK-29.

## Ce qui NE compte PAS comme preuve

Un pipeline avec des choix arbitraires non motivés ; une redéfinition
des fondamentaux conceptuels de FRK-29 au lieu d'une référence.

- Réservation d'ID : `FRK30.SKILL.FINGERPRINT_ENGINEERING.L1` —
  réservé, non émis.
- Prérequis structurel : FRK-29 (fondations), réutilisé par référence.
- Mapping `VALID_SIGNALS` : aucun aujourd'hui.
