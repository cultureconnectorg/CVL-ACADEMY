# FRK-31 — Affinity, Resonance & Cadence Signals

## Grounding

Per `FREK_01_75_RECONCILIATION.md`: coverage `NONE`, distinctness
`DISTINCT_SPECIALIZATION`, action `NEW_EXTERNAL`. **Boundary
(mandatory)**: this is a *different* signal vocabulary from the real
`VALID_SIGNALS` set (`FREK-TIME/WORK/SCORE/...`) defined in
`frek_core.py` — never conflate the two in content.

## Objectives

- Teach the market-general concept of affinity/resonance/cadence
  signals (engagement-pattern signals in cultural platforms) as a
  distinct professional vocabulary.
- Explicit, repeated boundary: this vocabulary is **not** implemented
  as `VALID_SIGNALS` anywhere in this repo — `frek_core.py`'s 8 real
  signal types are a different, narrower, already-shipped vocabulary.

## Modules

1. Affinity/resonance/cadence concept fundamentals.
2. Engagement-signal design practice.
3. Mandatory boundary discipline vs. real `VALID_SIGNALS`.

## Assessment

A concept exam with an eliminatory check on conflating this
vocabulary with `frek_core.py`'s real `VALID_SIGNALS`.

## Status

`STATUS = PACKAGE_COMPLETE` — full canonical package, deepened this
pass. Never implies `FULLY_COMPLETE`.
