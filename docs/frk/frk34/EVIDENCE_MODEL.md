# FRK-34 — Evidence Model

`READY_FOR_FREK_PROOF = FALSE`.

## Chaîne de preuve

Schéma d'identité d'œuvre annoté (hash, métadonnées structurelles) →
note de frontière LabelOS → correcteur → (jury si 2.0–2.5) →
`FRK34.SKILL.WORK_IDENTITY_TRACKING.L1` (réservé).

## Ce qui compte comme preuve

Un schéma d'identité purement technique (hash + métadonnées
structurelles) sans aucune clause de droits ou de catalogue ; la
frontière LabelOS correctement articulée comme structurelle et
permanente.

## Ce qui NE compte PAS comme preuve

Un schéma intégrant des champs de gestion de droits ; toute
affirmation que LabelOS/LOS-02 existe ou fonctionne d'une manière
précise aujourd'hui.

- Réservation d'ID : `FRK34.SKILL.WORK_IDENTITY_TRACKING.L1` —
  réservé, non émis.
- Frontière obligatoire : identité/provenance (FREK) vs. catalogue/
  droits (LabelOS LOS-02, zéro empreinte dépôt confirmée par
  `REPO_REGISTRY.md`) — jamais fusionnés, jamais dupliqués.
- Base pour FRK-35 (versions/dérivés/crédits) — réutilisé par
  référence.
- Mapping `VALID_SIGNALS` : aucun aujourd'hui.
