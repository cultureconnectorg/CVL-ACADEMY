# FRK-36 — Evidence Model

`READY_FOR_FREK_PROOF = FALSE`.

## Chaîne de preuve

Flux de capture authentique annoté (preuve à l'origine) + note
`NEEDS_REPO_AUDIT` + distinction avec `NEEDS_EXPERT_REVIEW` →
correcteur → (jury si 2.0–2.5) →
`FRK36.SKILL.AUTHENTIC_MEDIA_CAPTURE.L1` (réservé).

## Ce qui compte comme preuve

Un flux où la preuve d'intégrité est établie entièrement à l'origine ;
une note confirmant précisément que FREKRAW reste `NEEDS_REPO_AUDIT`,
distinct de `NEEDS_EXPERT_REVIEW`.

## Ce qui NE compte PAS comme preuve

Un flux où la preuve est ajoutée après la capture ; toute affirmation
d'un comportement précis de FREKRAW ; une confusion entre
`NEEDS_REPO_AUDIT` et `NEEDS_EXPERT_REVIEW`.

- Réservation d'ID : `FRK36.SKILL.AUTHENTIC_MEDIA_CAPTURE.L1` —
  réservé, non émis.
- `NEEDS_REPO_AUDIT` reste ouvert pour FREKRAW spécifiquement —
  recherche exhaustive confirmée sans résultat (`backend/`, `docs/`,
  `frekcoreAout2026`, `fms-os/fms`, `gmfest972/goodmooddjsayd`).
- Le contenu pédagogique lui-même (capture authentique, discipline
  marché-générale) est indépendant de ce statut et n'a pas besoin d'un
  audit pour être enseigné correctement.
- Mapping `VALID_SIGNALS` : aucun aujourd'hui.
