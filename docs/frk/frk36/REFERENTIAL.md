# FRK-36 — FREKRAW & Authentic Media Capture

## Grounding

Per `FREK_01_75_RECONCILIATION.md`: coverage `NONE`, distinctness
`DISTINCT_PROFESSION`, action `NEW_EXTERNAL`, but explicitly
`CAPABILITY_NOT_IMPLEMENTED` and **`NEEDS_REPO_AUDIT`** — FREKRAW is a
named sub-product with **zero visibility** anywhere accessible this
session (confirmed by exhaustive grep across `backend/`, `docs/`,
`frekcoreAout2026`, `fms-os/fms`, `gmfest972/goodmooddjsayd`).

## Objectives

- Teach the market-general concept of authentic media capture
  (tamper-evident capture at the point of origin) as an industry
  discipline — real, teachable independent of FREKRAW's own existence.
- Never claim FREKRAW itself has any specific implementation — its
  repo remains unfound; a future repo audit is required before any
  FREKRAW-specific claim can be made.

## Modules

1. Authentic-media-capture concept fundamentals (market-general).
2. Tamper-evidence-at-capture design principles.
3. `NEEDS_REPO_AUDIT` discipline — explicit statement that FREKRAW
   itself is unverified.

## Assessment

A concept exam graded against real media-capture-integrity practice,
never against an invented FREKRAW feature.

## Status

`STATUS = MODULE_CONTENT_DRAFTED`, `NEEDS_REPO_AUDIT` open for FREKRAW
specifically.
