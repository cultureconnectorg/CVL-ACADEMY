# FRK-40 — DAW Provenance & Creative Session Tracking

## Grounding

Per `FREK_01_75_RECONCILIATION.md`: coverage `NONE`, distinctness
`DISTINCT_SPECIALIZATION`, action `NEW_EXTERNAL`. Cross-reference
FMS-03 (DAW basics, Founder-gated canon) and FMS-08 (Recording
Engineering specialization, `docs/fms/fms08/`) — FMS teaches the
craft, FRK teaches the provenance layer on top. Explicit boundary
required, not a merge.

## Prerequisites

FMS-03 or FMS-08 literacy recommended.

## Objectives

- Teach real DAW-session provenance tracking (recording what happened
  in a production session, who touched what, when) as a distinct
  competency from the DAW craft itself.
- Explicit, mandatory boundary: this formation never re-teaches
  FMS-03/08's recording/production craft — it teaches the provenance
  layer on top.

## Modules

1. DAW-session provenance concepts.
2. Session-tracking practice (technical, not musical).
3. Boundary discipline vs. FMS-03/FMS-08 (by reference, never
   re-authored).

## Assessment

A provenance-tracking exercise with an eliminatory check on drifting
into re-teaching FMS-03/08 content.

## Status

`STATUS = PACKAGE_COMPLETE` — full canonical package, deepened this
pass. Never implies `FULLY_COMPLETE`.
