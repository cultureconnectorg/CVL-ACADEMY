# FRK-42 — Integration Academy Package Note

## Réel vs. supposé

**Réel :** la discipline de conception de transport de preuve
hors-ligne réelle (artefacts auto-vérifiables, chaînage par hash pour
la détection de gap, patterns USB/QR-code/data-diode) comme pratique
d'ingénierie de sécurité marché-générale.

**Supposé :** aucun transport de preuve hors-ligne CVLN observé —
`CAPABILITY_NOT_IMPLEMENTED`. `FRK42.SKILL.*` réel dans le runtime de
cette Academy — inexistant (`NO_RUNTIME_BINDING`).

## Dépendances

- `FREK_01_75_RECONCILIATION.md`, `docs/frk/frk20/REFERENTIAL.md`
  (frontière, jamais fusionnée), `70_EVIDENCE/
  EVIDENCE_ARCHITECTURE.md`.

## Ce qu'une future intégration exigerait

1. Une entrée `FRK42` dans le registre de certification de cette
   Academy, namespace distinct de `FRK20.SKILL.*`.
2. Une surface candidat réelle (ce corpus est markdown seul).
3. Un vrai artefact de transport à évaluer par un correcteur humain —
   inexistant aujourd'hui.

## Status

`STATUS = PACKAGE_COMPLETE` — référentiel, banques N1/N2, assessment
+ rubric, evidence model, 3 guides, cette note d'intégration existent
tous, deepened this pass. `FULLY_COMPLETE` non déclaré — requiert un
passage réel vérifié par un humain.
