# FRK-56 — FREK × KORA Integration

## Repo truth this formation is built on

Grounded in a real, already-documented cross-repo artifact — per
`FREK_01_75_RECONCILIATION.md`'s own verdict: curriculum coverage
`NONE (formal) / SUBSTANTIAL (documented boundary)`, occupational
distinctness `CROSS_ECOSYSTEM_ROLE`, action `NEW_CROSS_ECOSYSTEM`,
"richly grounded... reuse this documentation as the worked case, don't
re-derive":

- Every `docs/kor/korXX/` module already carries a real
  `FREK_PROOF_MAPPING` header, inherited from `seed_modules.py` for
  legacy-sourced modules.
- `docs/kor/kor01/skills/EVIDENCE_MODEL.md` declares
  `READY_FOR_FREK_PROOF = FALSE` on all 14 KOR-01 competencies without
  exception, with an explicit written rationale: the real
  `frek_signal` stack (`FREK-WORK`/`FREK-SCORE`/`FREK-CONTRIB`/
  `FREK-CERT`) is used, but no external verifiable anchor (published
  hash, third-party timestamp) exists anywhere in `fms_canonical`/
  `klt_canonical`/`frek_core.py` for any formation.

## Prerequisites

FRK-01, FRK-58, KOR-01 (or literacy of its evidence model).

## Objectives

FREK × KORA Integration is the professional competency of reading and
explaining the real, already-existing boundary between KORA's
pedagogical evidence model and the FREK proof stack — not building a
new integration, since none is missing to build:

- `FREK_PROOF_MAPPING` header literacy: reading a real KOR module
  header and correctly identifying which `VALID_SIGNALS` value(s) it
  documents.
- `READY_FOR_FREK_PROOF` literacy: explaining, from the real evidence
  model's own written rationale, exactly why every KOR competency is
  `FALSE` — never treating this as an oversight to "fix."
- Cross-ecosystem bridge discipline: this is the worked, real example
  of a KORA↔FREK relationship — reused, never re-derived, when other
  domains (FMS, KLT, Good Mood) need the same boundary explained.

## Modules

1. **`FREK_PROOF_MAPPING` header literacy** — grounded in real
   `docs/kor/korXX/modules/*.md` headers.
2. **`READY_FOR_FREK_PROOF = FALSE` rationale** — grounded in
   `docs/kor/kor01/skills/EVIDENCE_MODEL.md`'s own written explanation.
3. **Cross-ecosystem bridge reuse discipline** — explaining this
   worked case to a new domain without re-deriving the boundary from
   scratch each time.

## Assessment

A boundary-explanation exercise: candidate is given a real KOR module
header and evidence-model row and must correctly explain both the
signal-mapping and the `READY_FOR_FREK_PROOF = FALSE` rationale in
their own words — eliminatory failure for asserting a real external
anchor exists.

## Evidence / certification / mission eligibility

`FRK56.SKILL.*` Skill IDs, reserved once this formation is deepened to
full package. No mission eligibility path exists yet.

## Status

`STATUS = PACKAGE_COMPLETE` — full canonical package (référentiel +
N1/N2 + assessment/rubric + evidence model + 3 guides + integration
note), deepened this pass. Never implies `FULLY_COMPLETE` — no real
candidate has been assessed yet.
