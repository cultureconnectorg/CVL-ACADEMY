# FRK-58 — Guide Correcteur

## Avant de noter

Ouvre `backend/services/frek_core.py` à côté de la copie.

## Ce que tu vérifies en priorité

1. Le candidat décrit-il correctement le fallback silencieux (jamais
   d'erreur visible pour l'utilisateur) ?
2. Confond-il `frek_core.py` avec `frekcoreAout2026`/`frek_v3` ?
3. Invente-t-il un comportement d'erreur ou une intégration ?

## Ce que tu ne fais pas

Tu ne notes pas sur intuition générique — seulement sur le code réel
de `frek_core.py`.
