# FRK-60 — Evidence Model

`READY_FOR_FREK_PROOF = FALSE`.

## Chaîne de preuve

- Réservation d'ID : `FRK60.SKILL.FREK_INTELLIGENCE_OS_BRIDGE.L1` —
  réservé, non émis.
- Grounding réel : `frek_core.py` (client interne) et
  `services/integrations/registry.py` (config générique) —
  `CAPABILITY_NOT_IMPLEMENTED` des deux côtés, jamais présenté comme
  intégration fonctionnelle.
- Mapping `VALID_SIGNALS` : aucun aujourd'hui.
