# FRK-71 — FREK v3 Architecture

## Repo truth this formation is built on

Grounded in `cultureconnectorg/frekcoreAout2026`, commit
`fb272f1d491b09a6d068fb3f6c9c75d407bb0626`, `frek_v3/` — a real,
internally-consistent architecture corpus (already audited this
session, `REPO_REGISTRY.md`, G5/FD closure): `FREK_V3_Architecture_
Review_Final.md`, `FREK_Architecture_Integree_v0.2.md`,
`FREK_V3_Engineering_Exploded_View_v0.2.md`,
`FREK_V3_Reconciliation_Architecture_v0.2.md`, `CE_QUI_MANQUE.md`.

Per `FREK_01_75_RECONCILIATION.md`: coverage `PARTIAL — SOURCE_
OBSERVED`, distinctness `DISTINCT_PROFESSION`, action `NEW_EXTERNAL`.
**`NEEDS_FOUNDER_DECISION` = 0 — closed** on repo-verified maturity.

## Prerequisites

FRK-01 (recommended context — different, more mature layer of the same
eventual product).

## Objectives

- Teach the real FREK v3 architecture and its **own, explicit maturity
  ladder** — verbatim, never softened or inflated:
  `1. Concept (dépassé) → 2. Architecture (✅ on est ici, verrouillé)
  → 3. Engineering (⏳ prochaine phase, RTL/bits exacts/résultats
  expérimentaux)`. FPGA is named explicitly by the corpus itself as
  the bridge between Level 2 and Level 3 — **not yet crossed**.
- Standing classification, never deviated from: `ARCHITECTURE_
  LEVEL_2` / `SOURCE_OBSERVED`. Real architecture-level existence is
  never inflated into `NOT_FULL_ENGINEERING` (no RTL/timings),
  `NOT_HARDWARE_PROVEN` (no FPGA prototype), or `NOT_PRODUCTION_
  INTEGRATED` (no binding to any live FREKCORE, this Academy's
  `frek_core.py` included) being complete.
- Never confuse this layer with this Academy's own `frek_core.py`
  (FRK-01/58) — same eventual product, different, more mature, still
  not-production layer.

## Modules

1. FREK v3 architecture corpus literacy (the 5 real named documents).
2. Maturity-ladder literacy — verbatim 3-level model, FPGA as the
   named, uncrossed bridge to Level 3.
3. Boundary discipline vs. `frek_core.py` (FRK-01/58).

## Assessment

A maturity-classification exercise: candidate is given a real
capability claim about FREK v3 and must classify it correctly against
the corpus's own ladder — eliminatory failure for asserting Level 3
(engineering/hardware-proven) status.

## Evidence / mission eligibility

`FRK71.SKILL.*` reserved once deepened. No mission eligibility path
exists yet.

## Status

`STATUS = MODULE_CONTENT_DRAFTED`.
