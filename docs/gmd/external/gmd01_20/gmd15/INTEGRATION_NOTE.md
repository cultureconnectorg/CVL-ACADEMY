# GMD-15 — Integration Academy Package Note

## Réel vs. supposé

**Réel (cité par référence à GMD-25, jamais re-audité ni contredit
ici) :** `POST /scan/check`, `GET /scan/counter/{eid}`
(`server.py:722-760`), les 3 issues réelles de scan.

**Supposé :** un lien `GMD15.SKILL.*` réel dans le runtime de cette
Academy — inexistant (`NO_RUNTIME_BINDING`). Toute capacité de
récupération/incident automatisée — inexistante, GMD-34 reste un gap
déclaré. Tout protocole de sécurité non représenté dans le code
(surveillance de densité, coordination médicale/urgence) — enseigné
comme connaissance de marché, jamais comme capacité plateforme.

## Dépendances

- `docs/gmd/gmd25/` (exemple travaillé, jamais re-dérivé),
  `docs/gmd/gmd34/GAP.md` (limite d'escalade citée), `docs/
  cvln_academy_master/20_EXTERNAL/
  GOOD_MOOD_DJ_SAYD_RECONCILIATION.md`, `70_EVIDENCE/
  EVIDENCE_ARCHITECTURE.md`.

## Ce qu'une future intégration exigerait

1. Une entrée `GMD15` dans le registre de certification de cette
   Academy.
2. Une surface candidat réelle (ce corpus est markdown seul).
3. Un vrai rôle sécurité/accès à staffer — inexistant aujourd'hui.

## Status

`STATUS = PACKAGE_COMPLETE_FOR_GMD15` — référentiel, banques N1/N2,
assessment + rubric, evidence model, 3 guides, cette note
d'intégration existent tous. **Ceci ferme la portion GMD-01→20 de la
tâche #184** (3 flagships construits : GMD-05, GMD-14, GMD-15).
`FULLY_COMPLETE` non déclaré.
