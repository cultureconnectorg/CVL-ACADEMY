# GMD-29 — Guide Correcteur

## Avant de noter

Ouvre `server.py` (lignes 241-252) et `email_service.py` (lignes
64-82) à côté de la copie.

## Ce que tu vérifies en priorité

1. Le candidat a-t-il réellement vérifié le contenu de `"kr"` (créole
   haïtien) plutôt que de supposer coréen ?
2. A-t-il inventé une route de désinscription ou une colonne d'export ?
   Applique la règle éliminatoire sans exception.
3. Distingue-t-il bien `db.newsletter` de `db.fans` ?

## Ce que tu ne fais pas

Tu ne récompenses jamais une affirmation de support linguistique non
vérifiée contre le code réel.
