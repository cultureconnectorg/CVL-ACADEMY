# GMD-30 — Guide Correcteur

## Avant de noter

Ouvre `server.py` (lignes 389-410) à côté de la copie.

## Ce que tu vérifies en priorité

1. Le candidat cite-t-il le vrai calcul de `revenue_cents`
   (`sold * price_cents`, jamais une lecture de `payment_
   transactions`) ?
2. A-t-il identifié les deux caps distincts (2000 vs 5000) ?
3. A-t-il inventé un champ de ventilation ou une correction de
   formule ? Applique la règle éliminatoire sans exception.

## Ce que tu ne fais pas

Tu ne notes pas sur intuition produit — seulement sur le code réel.
