# docs/grp/ — CVLN Group Corpus (W6 Wave 14, Rail 1 priority 5)

```
Domain: CVLN Group (GRP-01→72) — 72 rows per the reconciliation's own
stated total; 63 itemized with a verdict, 9 unenumerated (flagged, not
invented — see GRP_GAP_NOTE.md).
Reconciliation source: docs/cvln_academy_master/30_INTERNAL/
                        FOUNDER_CEO_GROUP_FONDATION_RECONCILIATION.md
                        (0 rejected across all 158 rows in the parent
                        reconciliation).
```

## Real grounding + code-collision discipline

Legacy `GRP-01` ("Ecosystem Entrepreneurship", `backend/seed_data.py`,
28h, badge "Ecosystem Entrepreneur") and `GRP-02` ("Cultural Economy &
Strategic Partnerships", 26h) are real, delivered curriculum — never
rebuilt here. The Master 2D candidate general-business cluster also
numbers its rows `GRP-01→31,41→58`, colliding with the legacy code at
position 1 — this corpus's internal document code for the candidate
cluster is `GRP-EXT-01→...`, never bare `GRP-01`, same collision
discipline as `docs/hos/`'s `HOS-EXT-01` and `docs/los/`'s
`LOS-EXT-01→14`.

## Structure

- **`external/grp_general/`** — `GRP-EXT` general business pathway
  (39 rows per the reconciliation, `GRP-01→31,41→58` excl. `GRP-11`),
  `NEW_EXTERNAL`, market-general framework/process content.
- **`NEEDS_EXPERT_REVIEW.md`** — `GRP-11`, `GRP-32→40` (10 rows) —
  jurisdiction-specific corporate/tax/legal content, never a universal
  recipe.
- **`BLOCKED_CANDIDATES.md`** — `GRP-59→72` (14 rows, internal
  operator, no CVLN holding/governance system exists to operate).
- **`GRP_GAP_NOTE.md`** — 9 rows of the stated 72-row total left
  unenumerated by the source reconciliation's own prose — flagged for
  future audit, never invented.

## Status

| Set | Rows | Depth |
|---|---|---|
| GRP general (`external/grp_general/`) | 39 | `MODULE_CONTENT_DRAFTED` |
| `NEEDS_EXPERT_REVIEW.md` | 10 | `NEEDS_EXPERT_REVIEW`, no content |
| `BLOCKED_CANDIDATES.md` | 14 | `BLOCKED_PRODUCT_DEPENDENCY` |
| `GRP_GAP_NOTE.md` | 9 | `UNENUMERATED_IN_SOURCE_RECONCILIATION`, flagged |

**39 + 10 + 14 + 9 = 72.** All 72 rows of the domain's stated total
accounted for (63 with a real verdict, 9 flagged as a source-document
gap). 0/72 `PACKAGE_COMPLETE` (no flagship this wave — legacy
`GRP-01`/`GRP-02` are runtime content, not rebuilt), 39/72
`MODULE_CONTENT_DRAFTED`.

**Canonical state — never summarized otherwise:** this corpus is
**not** `PACKAGE_COMPLETE`, never asserts jurisdiction-specific
corporate/tax/legal content as a universal recipe, never simulates
CVLN's actual holding/governance structure, and never invents the 9
unenumerated rows' content.
