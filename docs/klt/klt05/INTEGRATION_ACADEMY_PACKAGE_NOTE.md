# KLT-05 — Note d'intégration Academy (package, pas import)

```
NO_RUNTIME_BINDING_YET. Voir klt01/INTEGRATION_ACADEMY_PACKAGE_NOTE.md
pour le raisonnement complet. Rappel renforcé pour cette formation :
même un futur import réel de ce package ne créerait, à lui seul, aucun
accès opérateur réel — cela resterait un système distinct
(OPERATOR_AUTHORIZATION), hors scope de tout import de contenu
pédagogique.
```

Même structure que les formations précédentes, namespace
`KLT05.SKILL.Cxx` distinct. Aucun fichier de `fms_import/`/
`fms_canonical/` modifié. Points ouverts identiques, non traités ici.
