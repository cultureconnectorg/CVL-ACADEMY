# KOR-03 — Note d'intégration Academy (package, pas import)

```
NO_RUNTIME_BINDING_YET.
```

Même patron que `KOR-01`/`KOR-02` (`INTEGRATION_ACADEMY_PACKAGE_NOTE.md`
de chaque formation). `KOR-03` référence en plus des ressources
produites par `KOR-01` (audio) et `KOR-02` (feature écrite) — un futur
`kor_canonical` devra permettre la référence croisée entre plus de deux
formations d'un même pôle, point déjà signalé par `KOR-02`'s note
équivalente et confirmé ici pour un troisième maillon.

Aucun code touché par ce ticket (`NO_RUNTIME_BINDING`,
`NO_KORA_PRODUCT_UPGRADE`).
