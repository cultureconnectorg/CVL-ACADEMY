# KOR-04 — Note d'intégration Academy (package, pas import)

```
NO_RUNTIME_BINDING_YET.
```

Même patron que `KOR-01`→`03`. `KOR-04` référence le catalogue produit
par `KOR-01`/`02`/`03` (*Rasin*) sans le modifier — confirme le besoin,
déjà signalé, d'une référence croisée inter-formations dans un futur
`kor_canonical`. Aucun code touché (`NO_RUNTIME_BINDING`,
`NO_KORA_PRODUCT_UPGRADE`).
