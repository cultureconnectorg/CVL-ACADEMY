# KOR-05 — Note d'intégration Academy (package, pas import)

```
NO_RUNTIME_BINDING_YET.
```

Même patron que `KOR-01`→`04`. `KOR-05` illustre le mieux, parmi les
formations construites jusqu'ici, la limite d'échelle d'une méthode
manuelle (M08) — un futur `kor_canonical` pourrait un jour porter
techniquement ce suivi de catalogue, mais cela relèverait d'un
`KORA_PRODUCT_UPGRADE`, explicitement hors scope de ce chantier.
Aucun code touché (`NO_RUNTIME_BINDING`).
