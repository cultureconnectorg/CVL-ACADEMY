# KOR-07 — Note d'intégration Academy (package, pas import)

```
NO_RUNTIME_BINDING_YET. NEEDS_EXPERT_REVIEW = TRUE.
```

Même patron que `KOR-01`→`06`. Aucun code touché
(`NO_RUNTIME_BINDING`). Point spécifique : un futur `kor_canonical` ne
devrait jamais interpréter automatiquement les statuts `NEEDS_EXPERT_
REVIEW` de ce corpus comme des données juridiques structurées
exploitables sans supervision humaine — cette réserve doit être
préservée dans toute future intégration technique.
