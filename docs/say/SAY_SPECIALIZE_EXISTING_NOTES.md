# SAY-01→10, SAY-21→28, SAY-34, SAY-39/40/43/44 — Specialize-Existing Framing Notes (23 rows)

```
STATUS = framing notes only, zero new formations. Each row
SPECIALIZE_EXISTING on a real, Founder-frozen FMS-01→06 canonical
formation — reusing its modules by reference, adding only the
independent/DJ-Sayd-specific competency layer. NO_DUPLICATE_
CURRICULUM.
```

## SAY-01→10 — Artist Identity & Independent-Artist Development (10 rows)

**Specializes `FMS-01`** (Diagnostic/Identité/Positionnement/
Storytelling/Roadmap). `COMPLETE`/`SUBSTANTIAL` curriculum coverage —
this is squarely FMS-01's subject. Distinctness: the "independent/DIY
artist-entrepreneur" lens (SAY-04, SAY-05, SAY-06) is a real,
`DISTINCT_SPECIALIZATION` within artist development (a specific
career-strategy segment, not a different profession) — same reasoning
already applied to FMS-08/09. Reuses FMS-01's modules by reference,
adding only the independent/DIY-specific competency layer.

## SAY-21→28 — Creative Music Production for Artists (8 rows)

**Specializes `FMS-03`** (Composition, Enregistrement & Édition
blocks). `SUBSTANTIAL` curriculum coverage. Distinctness: this is the
**artist's** creative-decision-making role (song development, vocal
direction, choosing takes/masters, collaborating with producers/
engineers) — genuinely distinct from FMS-03's *engineer/producer*
technical-skill role (same distinction already drawn for Mixing &
Mastering, mirrored here from the other side of the same
collaboration). Reuses FMS-03's technical modules by reference for the
parts an artist needs to understand but not execute.

## SAY-34 — Brand Partnerships & Sponsorships (1 row)

**Specializes `FMS-04`** — closer to FMS-04's branding/campaign domain
than to FMS-02's business domain.

## SAY-39, 40, 43, 44 — Performance Identity & Media Presence (4 rows)

**Specializes `FMS-04`** (styling/visual presence overlaps "Direction
visuelle," social-media-without-dilution overlaps "Réseaux sociaux").
`PARTIAL` coverage. Distinctness: on-camera/on-stage performance
coaching and media training is a real `DISTINCT_SPECIALIZATION` from
brand *strategy*.

## Summary

**23/23 rows resolved as `SPECIALIZE_EXISTING`, zero new formations.**
Each reuses its FMS-01→06 counterpart's modules by reference, adding
only the artist/independent/DJ-Sayd-specific competency layer named
above — never re-teaching the underlying FMS canon.
