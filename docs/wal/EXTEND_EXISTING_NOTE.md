# WAL-14 — Payment Security, Fraud & Risk (`EXTEND_EXISTING`, 1 row)

Per `WALLET_CVE_RECONCILIATION.md`: `WAL-14` joins the same
CyberSecure boundary question already resolved for `FRK-48-51`/
`KLT-17` (`G8`, `docs/cyb/EXTEND_EXISTING_NOTE.md`). One security
curriculum, referenced everywhere it is needed, never duplicated per
domain — `WAL-14` points to `docs/cyb/` (specifically `CYB-32`, the
one buildable `CYB-31→42` row) rather than re-teaching security
fundamentals inside the Wallet domain.

**No new content here** — this note exists only so a reader of
`docs/wal/` finds the pointer back to `docs/cyb/` rather than an
independently-drifting description of the same security boundary.
