# WAL-19 — Guide Correcteur

## Avant de noter

Ouvre `backend/wallet/models.py`, `service.py`, `backend/api/
wallet.py` à côté de la copie.

## Ce que tu vérifies en priorité

1. Le candidat distingue-t-il correctement CC (Academy) de JCC
   (Wallet) ?
2. Distingue-t-il ce ledger interne du vrai produit externe
   `djsayd/CVLN-Wallet` ?
3. A-t-il inventé une route de crédit direct ou un transfert ?
   Applique la règle éliminatoire sans exception.

## Ce que tu ne fais pas

Tu ne notes pas sur intuition produit — seulement sur le code réel.
