# XCV-57→66 — Intelligent Operations (10 rows, `MERGE` with `SYS-01→10`)

```
STATUS = converged, zero separate content. This is the clearest
over-counting case in the whole 812-row cartography this session: not
merely similar to SYS-01→10, but the SAME 10-stage pipeline restated
under a different domain header.
```

## Direct comparison

| XCV | Title | SYS (already reconciled, `docs/agf/EXTEND_EXISTING_NOTE.md`) |
|---|---|---|
| XCV-57 | CVLN Intelligent Operations | *(umbrella, matches the pipeline name itself)* |
| XCV-58 | Intent-to-Context | SYS-01→02 (Laurentia → IOS stages) |
| XCV-59 | Context-to-Plan | SYS-03 |
| XCV-60 | Plan-to-Agent | SYS-04 |
| XCV-61 | Agent-to-Application | SYS-05 |
| XCV-62 | Execution-to-Evidence | SYS-06 |
| XCV-63 | Evidence-to-Decision | SYS-07 |
| XCV-64 | Decision-to-Memory | SYS-08 |
| XCV-65 | Autonomous Operations | SYS-09 |
| XCV-66 | Human Takeover & Escalation | SYS-10 |

## Resolution

`SYS-01→10` already carries the verdict `EXTEND_EXISTING`/`MERGE`
against `MISSIONS_PIPELINES.md`'s own "Intelligent Operations"
doctrine (`docs/agf/EXTEND_EXISTING_NOTE.md`). `XCV-57→66` is that
**same** merge target, approached from the other domain sheet. **One
document, not two** — `MISSIONS_PIPELINES.md` remains the eventual
absorption point for the 10-stage breakdown once built, with both
`SYS-*` and `XCV-57→66` codes cross-referenced to it, never duplicated
as separate content.

**No content built here** — this note exists only so a reader of
either domain (`docs/agf/` or `docs/xcv/`) finds the same pointer,
never two independently-drifting descriptions of the same pipeline.
