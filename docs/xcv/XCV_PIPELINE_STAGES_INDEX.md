# XCV-11→56 — Pipeline Stage Decomposition (46 rows, `EXTEND_EXISTING`)

```
STATUS = stage index only, zero new content beyond the index itself.
Maps 1:1 onto the 6 pipelines already adopted as DECIDED doctrine in
60_CROSS_ECOSYSTEM/CROSS_ECOSYSTEM_MAP.md (DECIDED, not yet
implemented) — XCV-11→56 is the pedagogical unpacking of each stage,
never a re-derivation.
```

| Pipeline (already `DECIDED` doctrine) | XCV stage rows | Count | Real grounding cited per stage |
|---|---|---|---|---|
| Artist-to-Audience (artiste → FMS → FREK → LabelOS → KORA → public) | XCV-11 (umbrella) → XCV-18 | 8 | `FMS_07_18_RECONCILIATION.md` (FMS side), `FREK_01_75_RECONCILIATION.md` (FRK-56/58), `docs/los/`, `docs/kor/` — in that order, each stage citing only its own hand-off, never re-teaching the vertical's own content. |
| Activity-to-Value (activité → CVE → allocation → Wallet → settlement) | XCV-19 (umbrella) → XCV-26 | 8 | `WALLET_CVE_RECONCILIATION.md` — CVE is `FORMALIZED_METHODOLOGY` (`FD-CVE-001`), so stages touching CVE (XCV-21→23) are no longer Founder-blocked; any uncalibrated parameter stays `CALIBRATION_PENDING` at the module level. |
| Learning-to-Opportunity (apprentissage → preuve → qualification → mission) | XCV-27 (umbrella) → XCV-34 | 8 | The Academy's own real certification/skill/FREK-proof chain (`docs/kor/`, `FREK_01_75_RECONCILIATION.md`). |
| Digital-to-Physical (Academy → Spatial → lieu → mission réelle) | XCV-35 (umbrella) → XCV-42 | 8 | Each named vertical's own real repo grounding per stage (XCV-37 FMS, XCV-38 Good Mood, XCV-39/40 Gala/Hospitality — `docs/gcf/`/`docs/hos/`, XCV-41 Kiltikonet) — never re-teaching the vertical's own content, only the hand-off. |
| *(live-experience lens — cross-cut of Artist-to-Audience + Activity-to-Value, matching Mission B's Good Mood chain, not separately named in the 6)* | XCV-43 → XCV-50 | 8 | `GOOD_MOOD_DJ_SAYD_RECONCILIATION.md`'s real ticketing/booking/production chain — the best-grounded of the six pipeline families, since Good Mood is the most real-code-backed vertical in the whole cartography. |
| Cultural Memory (création → FREK → Fondation → archive → transmission) | XCV-51 (umbrella) → XCV-56 | 6 | `FOUNDER_CEO_GROUP_FONDATION_RECONCILIATION.md`'s Fondation Cœurvolan heritage content (`docs/fdc/`), `FD-CIP-001` respected throughout (CIP Foundation ≠ Fondation Cœurvolan, never merged). |

**8+8+8+8+8+6 = 46.** All 46 stage rows accounted for.

## Boundary discipline

Every stage cites its own vertical's real grounding **by reference**
— never re-derives or re-teaches the vertical's own competency
content. A stage teaches the **hand-off**, not the underlying craft
(e.g. XCV-37's Digital-to-Physical stage teaches how an FMS deliverable
hands off to a physical mission context, never re-teaching FMS
production skills themselves, which stay in `docs/fms/`).

## Summary

**46/46 rows resolved as `EXTEND_EXISTING`, zero new formations
beyond this stage-by-stage index.** Each pipeline's own `DECIDED`
doctrine (`CROSS_ECOSYSTEM_MAP.md`) is never rebuilt — only unpacked
into a teachable stage sequence.
